# fast-api-mongodb-flask-app

>pip install "fastapi[standard]"

>pip install uvicorn

>pip install pydantic

>pip install 'pydantic[email]'

>python -m pip install pymongo

>pip install pyjwt

>pip install itsdangerous